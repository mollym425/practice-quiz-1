git clone https://github.com/mollym425/practice-quiz-1.git
cd practice-quiz-1 
pwd
git status
touch commands.txt
mkdir -p project/{data,scripts,docs}
touch project/data/station0{1..5}.csv
cd project/scripts && touch clean.py analyse.py plot.py
mv project/data/station01.csv project/data/baseline.csv
mkdir project/backup && cp project/scripts/*.py project/backup
cd project && ls -R
cd .. && git add . && git commit -m "practice quiz part 1"
echo "North station" > project/docs/stations.txt && echo "South station" >> project/docs/stations.txt && echo "North tower" >> project/docs/stations.txt && echo "East station" >> project/docs/stations.txt
cat project/docs/stations.txt
grep "station" project/docs/stations.txt -n
grep "North" project/docs/stations.txt
tail -2 project/docs/stations.txt
wc -lw project/docs/stations.txt
grep "station" project/docs/stations.txt | wc -l
echo "temp/" > project/.gitignore
mkdir project/tmp && touch project/tmp/temp.txt
git status
git branch
git checkout -b add-summary
echo "Station review complete" > project/docs/summary.txt
git add project/docs/summary.txt
git commit -m "Add station summary"
git checkout master
find project/docs/summary.txt
git merge add-summary
find project/docs/summary.txt && cat project/docs/summary.txt
git branch -d add-summary
echo "Reviewed all 4 entries" >> project/docs/summary.txt
git add project/docs/summary.txt
git commit -m "Updat summary"
git commit --amend -m "Update summary"
echo "TODO: check station names" >> project/docs/summary.txt
git stash
cat project/docs/summary.txt
git stash pop
cat project/docs/summary.txt
git add . && git commit -m "updates"
git log --oneline
git checkout -b station-note
echo "This is a note" > project/docs/note.txt
git add project/docs/note.txt && git commit -m "Add note"
git checkout master
git diff master..station-note
git checkout station-note && git log --oneline
git checkout master
git cherry-pick 761dcf5
find project/docs/note.txt
git add .
git commit -m "final updates"
git push